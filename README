Usage:
	var linux	= require( "/path/to/this/directory/" );
	
Functionality:

	linux.mounts() returns an incremented array of mounted devices. 
	The indexes of the 2nd array are 'device', 'mountpoint', 'fs',
	and 'options'. Example below.

	[ [ device: '/dev/sda1',
	    mountpoint: '/',
	    fs: 'ext4',
	    options: 'rw,errors=remount-ro,commit=0' ] ]

	linux.ps() returns a detailed array of all the processes that
	are on the system. An incremented array, with the 2nd array
	containing indexes of:

	'name','state',tgid','pid','ppid',
	'tracerpid','uid','gid','fdsize','groups','vmpeak','vmsize',
	'vmck','vmhwm','vmrss','vmdata','vmstk','vmexe','vmlib',
	'vmpte','vmswap','threads','sigq','sigpnd','shdpnd','sigblk',
	'sigign','sigcgt','capinh','capprm','capeff','capbnd',
	'cpus_allowed','cpus_allowed_list','mems_allowed',
	'mems_allowed_list','voluntary_ctxt_switches', and 
	'nonvoluntary_ctxt_switches'.

	linux.ps.pid( pidNum ) returns a detailed array, containing
	the same indexes as above.

Tests:
	A simple test/example of what linux-util does is in tests/
